import Form from "../../components/Form/Form";
import CreatePageStyled from "./CreatePageStyles";

const CreatePage = (): JSX.Element => {
  return (
    <CreatePageStyled>
      <Form />
    </CreatePageStyled>
  );
};

export default CreatePage;
